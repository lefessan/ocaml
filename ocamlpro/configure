#!/bin/sh

COMMAND=$0
PWD=$(pwd)
OCAMLPRO=$PWD/$(dirname $COMMAND)

echo "# Generated by $COMMAND" > Makefile.root
echo >> Makefile.root
echo "ROOTDIR=$PWD" >> Makefile.root
echo "OCAMLPRO=$OCAMLPRO" >> Makefile.root
cat $OCAMLPRO/Makefile.trailer >> Makefile.root

cp -f $OCAMLPRO/config-ocp.h .
